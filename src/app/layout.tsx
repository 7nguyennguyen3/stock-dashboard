import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Stock Dashboard - Real-time Quotes",
  description:
    "Track real-time stock prices, changes, and percentage changes. Add symbols to your watchlist and sort the data dynamically.",
  keywords: [
    "stock quotes",
    "stock market",
    "real-time stocks",
    "finance",
    "dashboard",
    "Next.js",
    "Finnhub",
  ],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
